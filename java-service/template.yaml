apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: java-service-template
  title: "Java Spring Boot Service"
  description: "Plantilla para microservicio en Spring Boot"
spec:
  owner: java-team
  type: service
  steps:
    - id: fetch
      name: Obtener Java service
      action: fetch:template
      input:
        url: ./skeletons/springboot
        values:
          component_id: ${{ parameters.name }}
    - id: publish
      name: Publicar en GitHub
      action: publish:github
      input:
        repoUrl: github.com/org/${{ parameters.name }}
