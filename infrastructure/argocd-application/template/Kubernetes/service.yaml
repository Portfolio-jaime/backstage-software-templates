apiVersion: v1
kind: Service
metadata:
  name: ${{values.name}}-service
  namespace: ${{values.namespace}}
  labels:
    app: ${{values.name}}
spec:
  selector:
    app: ${{values.name}}
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP